# Nexus Repository Configuration
# Replace with your actual Nexus URL and repository name

# Set Nexus as the default registry
registry=https://your-nexus-server.com/repository/npm-public/

# Authentication (use auth token or username/password)
# Option 1: Using auth token
//your-nexus-server.com/repository/npm-public/:_authToken=${NEXUS_AUTH_TOKEN}

# Option 2: Using username/password (base64 encoded)
# //your-nexus-server.com/repository/npm-public/:username=${NEXUS_USERNAME}
# //your-nexus-server.com/repository/npm-public/:_password=${NEXUS_PASSWORD}

# For scoped packages (if you have private scoped packages)
# @your-scope:registry=https://your-nexus-server.com/repository/npm-private/

# Always use HTTPS
always-auth=true

# Cache settings
cache-min=10
